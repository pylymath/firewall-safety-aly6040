# Pinned to versions that have wheels for the Python the deploy target uses
# (Streamlit Community Cloud currently runs Python 3.14) and that match the
# environment the models were trained/saved in, so joblib/pickle loads cleanly.

# CPU-only PyTorch wheels: much smaller than the default CUDA build, so the
# install fits comfortably within Streamlit Community Cloud's resource limits.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.58.0
torch==2.12.0+cpu
transformers==5.12.1
sentence-transformers==5.6.0
scikit-learn==1.8.0
xgboost==3.2.0
lightgbm==4.6.0
pandas==3.0.3
numpy==2.4.5
matplotlib==3.10.9
joblib==1.5.3
huggingface_hub==1.16.1
jinja2==3.1.6
